# 使用项目本地的 pnpm store（而不是 H:\.pnpm-store 全局目录）
# - 避免 H 盘根级目录的权限/沙箱/被占用问题
# - 项目之间隔离，一个项目的 store 损坏不影响别的
store-dir=.pnpm-store

# pnpm v10+ 安全设置：允许以下依赖执行 postinstall/rebuild 脚本
#（better-sqlite3 需要它下载预编译的 better_sqlite3.node 原生二进制；
#  esbuild 需要它下载与当前平台匹配的 Go 原生二进制）
only-built-dependencies[]=better-sqlite3
only-built-dependencies[]=esbuild
